# GeoPostcodes Frontend Test

You can run the dev version using:

```
    npm run dev
```

Or the build version preview using:

```
    npm start
```

The compiled files are available in the dist folder
